package game

import (
	"testing"
)

func TestPlay(t *testing.T) {
	/*assert := assert.New(t)
	single := Play("S6")
	assert.True(single.Hit())
	assert.True(single.Single())
	sb := Play("SB2")
	assert.True(sb.StolenBase())*/
}
